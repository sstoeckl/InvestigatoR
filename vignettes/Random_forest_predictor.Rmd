---
title: "Random_forest_predictor"
author: "Mikhail Zhabin"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Vignette Title}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)

#' @Title Random Forest Predictor
#' This function trains a random forest model to predict a target variable based on input features and data
#' @param y The name of the target variable to predict. Should be a string
#' @param features A vector of feature names used for model training and prediction
#' @param data A dataframe containing the dataset including both the features and the target variable
#' @param ntree Number of trees to grow in the random forest. This affects the performance and accuracy of the model
#'
#' @return A vector of predictions for the first 5 instances in the testing sample
#'
#' @importFrom randomForest randomForest
#'
#'
#' @export
#'
#'
#' @examples
#'
#' random_foreest_predictor <- function(y, features, data, ntree ) {
#'   formula <- paste(y , paste(features, collapse = " + ")) # Defines the model
#'   formula <- as.formula(formula)                                   # Forcing formula object
#'   fit_RF <- randomForest(formula_C,         # New formula!
#'                            data = training_sample,    # Data source: training sample
#'                            sampsize = 20000,          # Size of (random) sample for each tree --> dependant on data/feature size
#'                            replace = FALSE,           # Is the sampling done with replacement?
#'                            nodesize = 250,            # Minimum size of terminal cluster --> dependant on data/feature size
#'                            ntree = 40,                # Number of random trees --> dependant on data/feature size
#'                            mtry = 30                  # Number of predictive variables for each tree --> dependant on data/feature size
#'   )
#' predictions <-  predict(fit_RF, testing_sample[1:5,])       # Prediction over the first 5 test instances
#'
#' return(predictions)
#' }

```

