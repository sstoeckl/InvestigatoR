% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio_objects.R
\name{postprocessing_portfolios}
\alias{postprocessing_portfolios}
\title{Post-Process Portfolio Weights for Multiple Models}
\usage{
postprocessing_portfolios(portfolio_object, config)
}
\arguments{
\item{portfolio_object}{A \code{portfolioReturns} object.}

\item{config}{A list containing post-processing configuration parameters such as weight constraints, regularization, leverage limits, and smoothing windows.}
}
\value{
An updated \code{portfolioReturns} object with adjusted weights and recalculated portfolio returns for each model.
}
\description{
Applies various constraints and adjustments to the portfolio weights based on a provided configuration.
Supports multiple models within a \code{portfolioReturns} object by processing each model independently.
}
\examples{
\dontrun{
# Define post-processing configuration
config <- list(
  list(
    operation = "set_weightsum",
    sum = 1,
    allow_short_sale = FALSE
  ),
  list(
    operation = "flatten_weights",
    l1 = 0.01,
    l2 = 0.01,
    mix = 0.7
  ),
  list(
    operation = "reduce_turnover",
    method = "exponential",
    smoothing_factor = 0.2
  ),
  list(
    operation = "increase_diversification",
    hh_target = 0.3
  )
)

# Apply post-processing
portfolio_obj <- postprocessing_portfolios(portfolio_obj, config)
}

}
